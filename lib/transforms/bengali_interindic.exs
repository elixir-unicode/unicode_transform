defmodule Unicode.Transform.BengaliInterindic do
  use Unicode.Transform

  # This file is generated. Manual changes are not recommended
  # Source: Bengali
  # Target: InterIndic
  # Transform direction: forward
  # Transform alias: 

  #
  # Bengali-InterIndic
  # VOWEL SIGN O
  replace("ো", "\uE04B")
  # VOWEL SIGN AU
  replace("ৌ", "\uE04C")
  # SIGN CANDRABINDU
  replace("ঁ", "\uE001")
  # SIGN ANUSVARA
  replace("ং", "\uE002")
  # SIGN VISARGA
  replace("ঃ", "\uE003")
  # LETTER A
  replace("অ", "\uE005")
  # LETTER AA
  replace("আ", "\uE006")
  # LETTER I
  replace("ই", "\uE007")
  # LETTER II
  replace("ঈ", "\uE008")
  # LETTER U
  replace("উ", "\uE009")
  # LETTER UU
  replace("ঊ", "\uE00A")
  # LETTER VOCALIC R
  replace("ঋ", "\uE00B")
  # LETTER VOCALIC L
  replace("ঌ", "\uE00C")
  # LETTER E
  replace("এ", "\uE00F")
  # LETTER AI
  replace("ঐ", "\uE010")
  # LETTER O
  replace("ও", "\uE013")
  # LETTER AU
  replace("ঔ", "\uE014")
  # LETTER KA
  replace("ক", "\uE015")
  # LETTER KHA
  replace("খ", "\uE016")
  # LETTER GA
  replace("গ", "\uE017")
  # LETTER GHA
  replace("ঘ", "\uE018")
  # LETTER NGA
  replace("ঙ", "\uE019")
  # LETTER CA
  replace("চ", "\uE01A")
  # LETTER CHA
  replace("ছ", "\uE01B")
  # LETTER JA
  replace("জ", "\uE01C")
  # LETTER JHA
  replace("ঝ", "\uE01D")
  # LETTER NYA
  replace("ঞ", "\uE01E")
  # LETTER TTA
  replace("ট", "\uE01F")
  # LETTER TTHA
  replace("ঠ", "\uE020")
  # LETTER DDA
  replace("ড", "\uE021")
  # LETTER DDHA
  replace("ঢ", "\uE022")
  # LETTER NNA
  replace("ণ", "\uE023")
  # LETTER TA
  replace("ত", "\uE024")
  # LETTER THA
  replace("থ", "\uE025")
  # LETTER DA
  replace("দ", "\uE026")
  # LETTER DHA
  replace("ধ", "\uE027")
  # LETTER NA
  replace("ন", "\uE028")
  # LETTER PA
  replace("প", "\uE02A")
  # LETTER PHA
  replace("ফ", "\uE02B")
  # LETTER BA
  replace("ব", "\uE02C")
  # LETTER BHA
  replace("ভ", "\uE02D")
  # LETTER MA
  replace("ম", "\uE02E")
  # LETTER YA
  replace("য", "\uE02F")
  # LETTER RA
  replace("র", "\uE030")
  # LETTER LA
  replace("ল", "\uE032")
  # LETTER SHA
  replace("শ", "\uE036")
  # LETTER SSA
  replace("ষ", "\uE037")
  # LETTER SA
  replace("স", "\uE038")
  # LETTER HA
  replace("হ", "\uE039")
  # SIGN NUKTA
  replace("়", "\uE03C")
  # SIGN AVAGRAHA
  replace("ঽ", "\uE03D")
  # VOWEL SIGN AA
  replace("া", "\uE03E")
  # VOWEL SIGN I
  replace("ি", "\uE03F")
  # VOWEL SIGN II
  replace("ী", "\uE040")
  # VOWEL SIGN U
  replace("ু", "\uE041")
  # VOWEL SIGN UU
  replace("ূ", "\uE042")
  # VOWEL SIGN VOCALIC R
  replace("ৃ", "\uE043")
  # VOWEL SIGN VOCALIC RR
  replace("ৄ", "\uE044")
  # VOWEL SIGN E
  replace("ে", "\uE047")
  # VOWEL SIGN AI
  replace("ৈ", "\uE048")
  replace("ো", "\uE04B")
  replace("ৌ", "\uE04C")
  #
  # SIGN VIRAMA
  replace("্", "\uE04D")
  # Khanda-ta
  replace("ৎ", "\uE083")
  # AU LENGTH MARK
  replace("ৗ", "\uE057")
  #
  # LETTER VOCALIC RR
  replace("ৠ", "\uE060")
  # LETTER VOCALIC LL
  replace("ৡ", "\uE061")
  # VOWEL SIGN VOCALIC L
  replace("ৢ", "\uE062")
  # VOWEL SIGN VOCALIC LL
  replace("ৣ", "\uE063")
  # DIGIT ZERO
  replace("০", "\uE066")
  # DIGIT ONE
  replace("১", "\uE067")
  # DIGIT TWO
  replace("২", "\uE068")
  # DIGIT THREE
  replace("৩", "\uE069")
  # DIGIT FOUR
  replace("৪", "\uE06A")
  # DIGIT FIVE
  replace("৫", "\uE06B")
  # DIGIT SIX
  replace("৬", "\uE06C")
  # DIGIT SEVEN
  replace("৭", "\uE06D")
  # DIGIT EIGHT
  replace("৮", "\uE06E")
  # DIGIT NINE
  replace("৯", "\uE06F")
  # Bengali-InterIndic: LETTER RA WITH MIDDLE DIAGONAL
  replace("ৰ", "\uE071")
  # Bengali-InterIndic: LETTER RA WITH LOWER DIAGONAL
  replace("ৱ", "\uE072")
  # Bengali-InterIndic: RUPEE MARK
  replace("৲", "\uE073")
  # Bengali-InterIndic: RUPEE SIGN
  replace("৳", "\uE074")
  # Bengali-InterIndic: CURRENCY NUMERATOR ONE
  replace("৴", "\uE075")
  # Bengali-InterIndic: CURRENCY NUMERATOR TWO
  replace("৵", "\uE076")
  # Bengali-InterIndic: CURRENCY NUMERATOR THREE
  replace("৶", "\uE077")
  # Bengali-InterIndic: CURRENCY NUMERATOR FOUR
  replace("৷", "\uE078")
  # Bengali-InterIndic: CURRENCY NUMERATOR ONE LESS THAN THE DENOMINATOR
  replace("৸", "\uE079")
  # Bengali-InterIndic: CURRENCY DENOMINATOR SIXTEEN
  replace("৹", "\uE07A")
  # ISSHAR
  replace("৺", "\uE07B")
  # DANDA
  replace("।", "\uE064")
  # DOUBLE DANDA
  replace("॥", "\uE065")
  # :: NFC (NFD) ;
  # eof
  #
end